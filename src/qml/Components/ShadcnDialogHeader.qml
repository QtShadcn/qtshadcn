import QtQuick

// shadcn/ui 风格 Dialog 头部：Title + Description 垂直堆叠（对齐 shadcn cn-dialog-header）
Column {
    id: root

    spacing: 6   // shadcn gap-1.5
    width: parent ? parent.width : implicitWidth
}
